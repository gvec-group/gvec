# Quantities for Postprocessing

GVEC provides a number of built-in quantities for postprocessing an equilibrium. These can be used with the `compute`, `evaluate` and `evaluate_sfl` functions.

(table-of-quantities)=
## Table of built-in quantities
```{include} /generators/quantities.md
```
